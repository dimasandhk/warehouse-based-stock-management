{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@warehouse-based-stock-management-oppo-technical-test/env": "workspace:*",
    "@warehouse-based-stock-management-oppo-technical-test/db": "workspace:*"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/cors": "^2.8.17",
    "tsx": "^4.19.2",
    "@types/node": "^22.13.14",
    "typescript": "catalog:",
    "tsdown": "^0.16.5",
    "@warehouse-based-stock-management-oppo-technical-test/config": "workspace:*"
  }
}
